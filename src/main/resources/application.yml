#config
#you should create MySQL database named "TestFinderDB" before starting this project
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/TestFinderDB
    username: root #mysql username
    password: 123456 #mysql password
  #h2:
    #console:
      #enabled: true

  jpa:
    generate-ddl: true

    hibernate:
      ddl-auto: update

    show-sql: true